{
  "description": "Warn once per session if the connected arknet MCP server is missing a tool a shipped skill needs, and nudge once per session if arknet's store was written but a discovered export snapshot in the project was not regenerated.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/check-arknet-compat.sh"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/export-freshness-nudge.sh"
          }
        ]
      }
    ]
  }
}
