{
  "description": "Warn once per session if the connected arknet MCP server is missing a tool a shipped skill needs.",
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "${CLAUDE_PLUGIN_ROOT}/hooks/check-arknet-compat.sh"
      }
    ]
  }
}
